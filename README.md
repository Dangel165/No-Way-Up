# No Way Up

Forge 1.20.1 기반 심리 공포 모드입니다.

## 현재 구현된 기능

- 처음 접속한 플레이어를 봉쇄된 폐광 방 안에 스폰시킵니다.
- 시작 폐광 방은 월드당 1회만 생성되어 멀티플레이에서 새 플레이어가 들어와도 구조를 초기화하지 않습니다.
- 시작 폐광에 보급 상자를 추가하고, 현재 접속 인원 수만큼 철 곡괭이를 채웁니다.
- 월드 `SavedData`를 사용해 플레이어별 공포 진행도를 저장합니다.
- 플레이어 주변에서 불길한 바닐라 사운드를 재생합니다.
- 영어 공포 속삭임과 자막형 액션바 메시지를 표시합니다.
- 바탕화면에 `READ_ME_NOWAYUP.txt` 텍스트 파일을 1회 생성합니다.
- 중반 진행 중 재접속을 유도하기 위해 자동 강제 크래시를 1회 발생시킵니다. 멀티플레이에서는 서버 전체를 터뜨리지 않고 해당 플레이어만 연결 해제합니다.
- 시작 폐광의 여러 상자에 영어 스토리/로어 책을 나누어 배치합니다.
- 플레이어 머리와 어두운 장비를 사용한 임시 Watcher 환영을 앞이나 뒤에 생성합니다.
- 위로 탈출하려는 시도를 새로 생성된 더 깊은 폐광 세그먼트로 되돌립니다.
- 창고, 숙소, 무너진 길, 스컬크 제단, 갈라진 레일, 가짜 출구, 표지판, 메시지 상자 등 다양한 폐광 세그먼트를 생성합니다.
- 재접속 후 시작 폐광을 변화시킵니다: 차가운 횃불, 열린 문, 새 표지판, 메시지 책.
- 거울 폐광용 `nowayup:mirror_mine` 차원을 등록하고 사용합니다.
- 거울 폐광에서 지연된 발소리, 반전 표지판, 거꾸로 적힌 플레이어 이름, 가짜 하강문 이벤트가 발생합니다.
- 블록 변화, 사운드, 암흑 효과, 경로 폐쇄가 포함된 단계별 거울 폐광 붕괴 이벤트를 실행합니다.
- 월드 변화와 상태 효과가 포함된 Loop, Descent, Replacement 엔딩을 지원합니다.
- 거울 폐광 내부의 몬스터 스폰을 억제합니다.

## 공포 파일 연출 안내

일반 진행 중 모드는 사용자의 바탕화면에 `READ_ME_NOWAYUP.txt` 파일을 1회 생성합니다. 실행 파일, 스크립트, 시작프로그램 항목은 생성하지 않으며 기존 파일을 덮어쓰지 않습니다.

## 스토리

전체 영어 스토리, 연표, 로어 노트, 엔딩 콘셉트는 [STORY.md](STORY.md)에 있습니다.
한국어 번역본은 [STORY_KO.md](STORY_KO.md)에서 볼 수 있습니다.

## 테스트 명령어

OP 권한이 필요합니다.

```text
/nowayup start
/nowayup fear get
/nowayup fear add 160
/nowayup fear set 220
/nowayup desktop
/nowayup crash
/nowayup watcher
/nowayup lore
/nowayup mutate
/nowayup mirror
```

빠른 테스트 추천 순서:

```text
/nowayup start
/nowayup mutate
/nowayup fear set 260
/nowayup watcher
/nowayup watcher
/nowayup watcher
/nowayup mirror
```

이 모드는 진행 중 의도적으로 실제 런타임 예외를 발생시킬 수 있습니다. `/nowayup crash`는 같은 방식의 크래시를 수동으로 테스트하는 명령어입니다.

## 멀티플레이

6명 멀티플레이를 기준으로 플레이어별 공포 진행도, 재접속 횟수, Watcher 관측 횟수, 거울 차원 진입 여부를 따로 저장합니다. 시작 폐광은 월드당 1회만 생성되며, 새 플레이어가 들어와도 기존 폐광 상태를 다시 덮어쓰지 않습니다.

새 플레이어가 접속할 때마다 시작 폐광의 보급 상자를 확인하고, 현재 접속 인원 수보다 철 곡괭이가 적으면 부족한 만큼 채웁니다. 6명이 모두 접속하면 보급 상자에는 철 곡괭이가 6개까지 준비됩니다.

자동 크래시 이벤트는 싱글플레이에서는 실제 런타임 예외를 발생시키고, 멀티플레이에서는 서버 전체가 내려가지 않도록 해당 플레이어만 공포 메시지와 함께 연결 해제합니다. `/nowayup crash` 명령어는 관리자 테스트용 실제 서버 크래시 명령어이므로 멀티 서버에서는 주의해서 사용해야 합니다.

## 시작 방법

### 싱글플레이

1. `build/libs/nowayup-0.1.0.jar` 파일을 Minecraft Forge 1.20.1의 `mods` 폴더에 넣습니다.
2. Forge 1.20.1 프로필로 게임을 실행합니다.
3. 새 월드를 만들거나 기존 월드에 접속합니다.
4. 월드에 들어가면 자동으로 폐광 시작 방으로 이동하며 모드가 시작됩니다.

### 멀티플레이

1. 서버와 모든 플레이어 클라이언트에 같은 `nowayup-0.1.0.jar` 파일을 넣습니다.
2. Forge 1.20.1 서버를 실행합니다.
3. 플레이어가 서버에 접속하면 자동으로 폐광 시작 방으로 이동합니다.
4. 시작 방의 보급 상자에서 각자 철 곡괭이를 하나씩 가져가면 됩니다.

## 빌드

```powershell
./gradlew build
```

Gradle이 Forge를 처음 다운로드할 때는 네트워크 연결이 필요합니다.
